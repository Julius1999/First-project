<!DOCTYPE html>
<html>

<head>
  <title>
    
  </title>
  
  <%= render 'layouts/shim' %>
</head>

<body>
  <%= render 'layouts/header' %>
    <div class="container">
      <%= yield %>
      <% render 'layouts/_footer' %>
      <!-- 限制只在开 发环境中显示调试信息。 -->
      <% debug(params) if Rails.env.development? %>
    </div>
</body>

</html>